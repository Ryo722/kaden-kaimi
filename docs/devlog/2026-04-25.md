# 2026-04-25 開発記録

Phase 2（自動化 / Workers Cron + API 連携 + データ拡充）着手日。

## 進捗サマリ

| サブタスク | 状態 | コミット |
|---|---|---|
| P2.1 前提整備 | ✅ 完了（P2.1.5 wrangler secret は P2.5 で実施） | — |
| P2.2 Workers スキャフォールド | ✅ 完了 | `86cea44` |
| P2.3 楽天 / Yahoo! API クライアント | ✅ 完了 | `8d23fba` |
| P2.4 GitHub Contents API + パイプライン | ✅ 完了 | `afdd73f` |
| P2.5 ローカルドライラン + 本番有効化 | 🟡 ステップ A 完了、データ品質課題で停止 | (ドライラン commit `8205597` on `phase-2-dryrun`) |
| P2.6 / P2.7 / P2.8 | ⬜ 未着手 | — |

main ブランチは `afdd73f` 時点。origin/main に対して 4 コミット先行（next session で `git push` 予定）。

## 実装した内容

### P2.1 前提整備
- 楽天 applicationId、Yahoo! Client ID、GitHub Fine-grained PAT（`Ryo722/kaden-kaimi` 限定 / `Contents: Read and write`）を発行・取得（ユーザー作業）
- `.dev.vars.example` テンプレートを `workers/fetch-prices/` 配下に配置（コミット可能）
- 実シークレットは `workers/fetch-prices/.dev.vars` に配置（gitignore 済み）
- 既存 5 機種の `externalIds` 整備方針: **案 B（null 継続、keyword フォールバック）** を採用。判明分は P2.5 ドライランログから後追い記入する方針

### P2.2 Workers スキャフォールド
- `pnpm-workspace.yaml` で `workers/*` を pnpm モノレポ化
- `workers/fetch-prices/` に wrangler 4.84 / `@cloudflare/workers-types` / vitest / zod を分離
- `wrangler.toml`: cron `0 20 * * *`（JST 05:00）、`nodejs_compat`、`[vars]` で公開設定、`[observability]` 有効
- ルート `tsconfig.json` の exclude に `workers` を追加（astro check と分離）
- ESLint flat config に Worker globals 対応・`**/dist/**` 除外を追加、`globals` パッケージ導入
- Astro Cloudflare adapter が生成する `.wrangler/deploy/` との衝突回避用 `clean:astro-deploy` pre-script を導入

### P2.3 楽天 / Yahoo! API クライアント
- `src/http.ts`: `fetchWithRetry`（最大 3 試行、`attempt^2 * 1000ms` バックオフ = 1s, 4s）、`sleep`、`FetchError`
- `src/rakuten.ts`: 楽天 IchibaItem 商品検索（itemCode 優先、未指定なら modelNumber を keyword 検索）
- `src/yahoo.ts`: Yahoo! ショッピング V3 商品検索（`in_stock=true`、yahooItemCode 優先 query）
- `src/types.ts`: 共通の `PriceQuote` 型（min / avg / available / hitCount / topItemCode）
- `vitest.config.ts`: v8 カバレッジ閾値 80%
- 単体テスト 34 ケース（http 10 / rakuten 12 / yahoo 12）

### P2.4 GitHub Contents API + パイプライン
- `src/github.ts`: `getFile` / `putFile` / `listDirectory`、`nodejs_compat` の `Buffer` で UTF-8 セーフ base64
- `src/pipeline.ts`: 列挙 → 並列取得 → zod 二段検証 → 冪等性チェック → 追記コミット、機種間 1s sleep、`categoryError` で list 失敗を per-model failure と分離
- shared `src/types/schema.ts` を Workers から import（tsconfig include 拡張、`@types/node` 追加）
- `index.ts` scheduled ハンドラに pipeline 結線、JSON 構造化ログ
- 単体テスト 22 ケース（pipeline 13 / github 16）

**最終テスト合計**: 200 passed（root 133 + workers 67）
**Workers カバレッジ**: stmts 93.09% / branches 85.71% / funcs 93.54% / lines 95.2%
**Workers バンドル**: 549.81 KiB / gzip 82.80 KiB（Workers Free 1 MB 制限の 8%）

## 重要な設計判断

| 日付 | 決定 | 理由 |
|---|---|---|
| 2026-04-25 | 自動コミットは **A 案（main 直 push）** | 反映速度最優先、Cloudflare Pages のビルド数最小、データ汚染防止は zod 二段検証＋追記のみで担保 |
| 2026-04-25 | 共通 zod スキーマは Workers から相対 path で import | 別パッケージ化・symlink・コピーよりシンプル。esbuild は include 外も依存解決可能 |
| 2026-04-25 | `nodejs_compat` フラグ採用 | `Buffer` で UTF-8 セーフ base64 が必要（GitHub Contents API 経路） |
| 2026-04-25 | `categoryError` を summary に追加 | リスト失敗（list 自体が落ちる）と per-model 失敗を分離して観測しやすくする |

## P2.5 ドライランの結果

`phase-2-dryrun` ブランチで実 API 経由のドライランを実施（2026-04-25 23:21 JST）。

**機能性**: 全項目期待通り
- HTTP 200、約 2 秒で完走
- TARGET_MODEL_ID フィルタで 1 機種に絞れた
- GitHub 自動コミット成功（`8205597`、author `kaden-kaimi-bot`、commit message `chore(prices): update drum-washer/panasonic-na-lx129dl for 2026-04-25`）
- 冪等性 OK（2 回目は `skipped_duplicate` でコミット重複なし）

**データ品質**: ⚠️ 修正必須
- 書き込まれたレコード: `rakutenMin: null, yahooMin: 1320, yahooAvg: 3987`
- Yahoo! の top hit が部品ショップ「andonya」の `y_n-gy1x10`（実機ではない部品）
- 楽天はヒット 0 件
- 原因: `keyword=NA-LX129DL` の単純検索だと、品番が含まれる関連商品（部品・取説等）も拾ってしまう

## 次セッションの最優先タスク

1. **P2.5.D1〜D4: 価格下限フィルタの実装**
   - `src/lib/constants.ts` に `CATEGORY_PRICE_FLOOR = { "drum-washer": 50000 }` を追加
   - `workers/fetch-prices/src/rakuten.ts` と `yahoo.ts` の `parseRakutenPayload` / `parseYahooPayload` でフィルタ適用
   - `RakutenSearchInput` / `YahooSearchInput` に `minPrice` を追加（pipeline 側で渡す）
   - 単体テストで「下限以下を除外」を検証
   - `phase-2-dryrun` ブランチで再ドライラン

2. **P2.5.B: 本番デプロイ**（D 完了後）
   - `.dev.vars` の `GITHUB_BRANCH` / `TARGET_MODEL_ID` 上書きを削除
   - `wrangler secret put` で本番 secret 登録 × 3
   - `wrangler deploy` で本番投入
   - 手動トリガで動作確認

3. **P2.5.C: クリーンアップ** + **P2.6 データカバレッジ拡大**

## セッション終了時の状態

- main: `afdd73f`、origin/main に対して 4 コミット先行
- phase-2-dryrun: `8205597`（ドライラン commit 1 件）→ **保持**（次回再ドライラン時に再利用）
- `.dev.vars`: `GITHUB_BRANCH=phase-2-dryrun` と `TARGET_MODEL_ID=panasonic-na-lx129dl` の上書き継続中（次回もそのまま使える）
- バックグラウンドプロセス: なし（wrangler dev 停止済み）
- ワーキングツリー: クリーン（このコミット直前時点）
- API quota 消費: 楽天 2 / Yahoo! 2（A-3 と A-5 で 2 回叩いた）
