# 2026-04-27 開発ログ

## Lighthouse CI 2 段構え化（spec: 20260426-lighthouse-ci-two-tier）

ブランチ `feat/lighthouse-two-tier` を `origin/main` から切って、T-001〜T-005 を実装。

### 実装内容（コミット順）

1. **T-004** `ci(lighthouse): PR 限定化 + numberOfRuns を 3 に増やす`
   - `.github/workflows/lighthouse.yml` を `pull_request` のみに（`on.push` 削除）
   - `LHCI_GITHUB_APP_TOKEN` の条件式を簡素化
   - `.lighthouserc.json` の `numberOfRuns` を 2 → 3

2. **T-003** `ci(lighthouse): preview URL 用 lhci 設定を追加`
   - `.lighthouserc.preview.json` 新規作成
   - `staticDistDir` / `url` は持たず、workflow 側から `--collect.url` で渡す
   - `skipAudits: ["uses-http2"]` を外す（CF は HTTP/2/3 配信のため audit が pass する想定）

3. **T-001 + T-002** `ci(lighthouse): preview URL 計測 workflow を追加`
   - `.github/workflows/lighthouse-preview.yml` 新規作成
   - main push / nightly schedule (JST 04:00) / workflow_dispatch で起動
   - GitHub Deployments API を polling して SHA 一致の Production deployment を待つ
   - 30s × 10 回（最大 5 分）→ 60s sleep + 再試行 → 本番 URL fallback
   - フォールバック時はアーティファクト名 `-fallback` サフィックス + job summary で識別

4. **T-005** `docs(lighthouse): 2 段構成の運用方針を明記`
   - `docs/architecture.md` に「CI/CD 構成」節を追加
   - `README.md` の Lighthouse CI メモを 2 段構え説明に差し替え

### code-reviewer 指摘の反映

- HIGH: environment 名の正規表現を case-insensitive に（`test("production|cloudflare"; "i")`)
- MEDIUM: `LHCI_GITHUB_APP_TOKEN` を `push` trigger 時のみに限定
- MEDIUM: フォールバック時に `is_fallback=true` output を立て、アーティファクト名と job summary に反映

### 重要な発見: GitHub Deployments API が空

`gh api repos/Ryo722/kaden-kaimi/deployments` が空配列を返す。CF Pages 側の GitHub App が Deployments 通知権限を持っていない可能性。

**現状の挙動**: polling は常にタイムアウトし、`https://kaden-kaimi.pages.dev` 固定 fallback で計測される（SHA 不一致リスクあり）。

**次アクション**（T-002 の next_action）: CF Pages dashboard で GitHub App の Deployments 通知設定を確認・有効化。有効化後は polling が正常に機能する設計のため、workflow 側は変更不要。

### 残タスク

- 本ブランチの PR 作成 → main マージで実 workflow の起動確認（T-001/T-004 の verify）
- CF Pages 側設定確認（T-002 の ops）

### phase-2-dryrun との関係

並行で `phase-2-dryrun` ブランチで P2.5.B 本番デプロイが進行中。本セッションでは触らず、`feat/lighthouse-two-tier` のみで作業した。コンフリクト想定領域なし（phase-2-dryrun は `docs/deploy-checklist.md` 等、本ブランチは `.github/workflows/`, `.lighthouserc*.json`, `docs/architecture.md` 末尾追記）。
