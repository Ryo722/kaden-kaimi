# 2026-04-27 開発ログ

## Lighthouse CI 2 段構え化（spec: 20260426-lighthouse-ci-two-tier）

ブランチ `feat/lighthouse-two-tier` を `origin/main` から切って、T-001〜T-005 を実装。

### 実装内容（コミット順）

1. **T-004** `ci(lighthouse): PR 限定化 + numberOfRuns を 3 に増やす`
   - `.github/workflows/lighthouse.yml` を `pull_request` のみに（`on.push` 削除）
   - `LHCI_GITHUB_APP_TOKEN` の条件式を簡素化
   - `.lighthouserc.json` の `numberOfRuns` を 2 → 3

2. **T-003** `ci(lighthouse): preview URL 用 lhci 設定を追加`
   - `.lighthouserc.preview.json` 新規作成
   - `staticDistDir` / `url` は持たず、workflow 側から `--collect.url` で渡す
   - `skipAudits: ["uses-http2"]` を外す（CF は HTTP/2/3 配信のため audit が pass する想定）

3. **T-001 + T-002** `ci(lighthouse): preview URL 計測 workflow を追加`
   - `.github/workflows/lighthouse-preview.yml` 新規作成
   - main push / nightly schedule (JST 04:00) / workflow_dispatch で起動
   - GitHub Deployments API を polling して SHA 一致の Production deployment を待つ
   - 30s × 10 回（最大 5 分）→ 60s sleep + 再試行 → 本番 URL fallback
   - フォールバック時はアーティファクト名 `-fallback` サフィックス + job summary で識別

4. **T-005** `docs(lighthouse): 2 段構成の運用方針を明記`
   - `docs/architecture.md` に「CI/CD 構成」節を追加
   - `README.md` の Lighthouse CI メモを 2 段構え説明に差し替え

### code-reviewer 指摘の反映

- HIGH: environment 名の正規表現を case-insensitive に（`test("production|cloudflare"; "i")`)
- MEDIUM: `LHCI_GITHUB_APP_TOKEN` を `push` trigger 時のみに限定
- MEDIUM: フォールバック時に `is_fallback=true` output を立て、アーティファクト名と job summary に反映

### 重要な発見: GitHub Deployments API が空

`gh api repos/Ryo722/kaden-kaimi/deployments` が空配列を返す。CF Pages 側の GitHub App が Deployments 通知権限を持っていない可能性。

**現状の挙動**: polling は常にタイムアウトし、`https://kaden-kaimi.pages.dev` 固定 fallback で計測される（SHA 不一致リスクあり）。

**次アクション**（T-002 の next_action）: CF Pages dashboard で GitHub App の Deployments 通知設定を確認・有効化。有効化後は polling が正常に機能する設計のため、workflow 側は変更不要。

### 残タスク

- 本ブランチの PR 作成 → main マージで実 workflow の起動確認（T-001/T-004 の verify）
- CF Pages 側設定確認（T-002 の ops）

### phase-2-dryrun との関係

並行で `phase-2-dryrun` ブランチで P2.5.B 本番デプロイが進行中。本セッションでは触らず、`feat/lighthouse-two-tier` のみで作業した。コンフリクト想定領域なし（phase-2-dryrun は `docs/deploy-checklist.md` 等、本ブランチは `.github/workflows/`, `.lighthouserc*.json`, `docs/architecture.md` 末尾追記）。

---

## Lighthouse Check Runs polling 切替（spec: 20260427-lighthouse-check-runs-polling）

ブランチ `feat/lighthouse-check-runs-polling` を `origin/main` から切り、T-006/T-007 を実装。

### 背景

PR #5 の T-002 stop_reason で判明した CF Pages の API 仕様（GitHub App `cloudflare-workers-and-pages` は Check Runs API のみ書き込み、Deployments / Statuses API には書かない）に正面から対応。

旧 spec `20260426-lighthouse-ci-two-tier` の Deployments API polling は構造的に常に空配列で fallback 経路に落ちるため、Check Runs API polling に切り替え、SHA 一致を `gh api commits/$SHA/check-runs` で厳密に確認する設計に変更。

### 実装内容（コミット順）

1. `docs(spec): add 20260427-lighthouse-check-runs-polling`
   - `openspec/proposals/20260427-lighthouse-check-runs-polling/spec.md` 新規作成
   - frontmatter に `supersedes: 20260426-lighthouse-ci-two-tier` を明記
   - delta は MODIFIED 中心（lighthouse-preview.yml の polling 書き換え + docs 更新）
   - `tasks.json` の `active_specs` を新 spec ID に切替、T-006/T-007 を pending で追加

2. **T-006** `ci(lighthouse): switch preview polling to Check Runs API`
   - `.github/workflows/lighthouse-preview.yml` の Wait ステップを書き換え
   - `permissions`: `deployments: read` → `checks: read`
   - step id: `deployment` → `cf-build`、step 名: `Wait for Cloudflare Pages build`
   - jq filter: `.check_runs[] | select(.app.slug == "cloudflare-workers-and-pages" and .name == "Cloudflare Pages")` の最新 1 件
   - `conclusion` 失敗系（`failure` / `timed_out` / `cancelled` / `action_required` / `stale` / `neutral` / `skipped`）はすべて `exit 1` で workflow fail
   - `check_runs` 配列が完全に空のときのみ既存仕様通り `is_fallback=true` で `https://kaden-kaimi.pages.dev` fallback
   - 計測 URL は `pages.dev` 固定（CF Pages の標準動作で main 最新成功ビルドにエイリアスされるため）
   - output: `preview_url` → `measure_url` にリネーム、env: `PREVIEW_URL` → `MEASURE_URL`、`FALLBACK_URL` → `PAGES_URL`

3. **T-007** `docs(architecture): update CI/CD section for Check Runs polling`
   - `docs/architecture.md` の preview URL 解決説明を Check Runs API 方式に書き換え
   - 計測 URL を `pages.dev` 固定にする理由を明記
   - `README.md` の Lighthouse CI メモも追従更新

4. `chore(tasks): close T-006/T-007 after implementation`
   - tasks.json で T-006/T-007 を `state: implemented` に更新

### 設計判断（codex 助言反映）

- **`neutral` / `skipped` も fail 扱い**: GitHub Actions 依存関係上は成功扱いだが「main HEAD が Pages に反映済み」の証明にはならないため、保守的に fail させる
- **空配列 fallback は維持**: ユーザー指示「現状維持」を尊重し、CF 側 GitHub App 障害等の極端ケース用セーフティネットとして残す。ただし常用される設計ではないため `is_fallback=true` フラグを強く可視化（artifact suffix + job summary 警告文）
- **将来検討事項（次回 spec 改訂候補）**: codex は「空配列 fallback は schedule 限定 degraded mode にすべき。push trigger では fail させる方が CI 健全性が高い」と助言。今回はスコープ外として devlog にメモのみ

### 旧 spec の扱い

旧 spec `20260426-lighthouse-ci-two-tier` の物理ファイル（`openspec/proposals/20260426-*/spec.md`）は phase-2-dryrun ブランチにのみ存在し、本ブランチからは触れない。新 spec の `supersedes` frontmatter で関係を明示し、`tasks.json` の `active_specs` から旧 ID を除外。

phase-2-dryrun が main にマージされた**後**、別 PR で旧 spec の `status: archived` 更新を実施する（フォローアップタスクとして残す）。

### 残タスク / 既知制約

- main マージ後の verify: job summary に `Fallback used: false` 表示、polling が `in_progress` → `success` 遷移すること（T-006 の next_action）
- 旧 spec の archived 化は phase-2-dryrun merge 後の follow-up PR

### phase-2-dryrun との競合確認

- phase-2-dryrun の merge-base（`f1dcfa1`）は PR #4 より前。`lighthouse-preview.yml` を持っていないため、本ブランチでの書き換えと**直接コンフリクトしない**
- `tasks.json` は両ブランチが触るため将来 merge 時の競合は避けられない（phase-2-dryrun は古い `state: pending` を持っている）。phase-2-dryrun 側の責務で解消する想定
- `openspec/proposals/` は別サブディレクトリ（`20260426-*` vs `20260427-*`）の追加なので git 上は unrelated trees としてクリーンマージ可能

---

## PR #6 マージ後の verify（spec: 20260427-lighthouse-check-runs-polling）

ブランチ `chore/verify-check-runs-polling` を `origin/main` から切って、T-006/T-007 の verify を実施。

### 対象 run

- workflow: `Lighthouse CI (preview URL)`
- run id: `24979704717`
- 対象 commit: `b91623d`（PR #6 マージコミット）
- event: `push`（main へのマージで自動起動）
- 結果: `success`（全 step success）

### Check Runs polling の遷移確認

```
Attempt 1/10: polling check_runs for b91623d...
  check_run status=in_progress conclusion=
Attempt 2/10: polling check_runs for b91623d...
  check_run status=in_progress conclusion=
Attempt 3/10: polling check_runs for b91623d...
  check_run status=completed conclusion=success
```

CF ビルドが約 60 秒（Attempt 1 開始 06:20:54 → Attempt 3 解決 06:21:55）で `success` 化し、polling は `in_progress` を 2 回経て成功で抜けた。`max_attempts*interval = 300s` の 60s sleep + retry には到達せず、`is_fallback=false` 経路で計測を実行。

### 計測実行確認

- `Run Lighthouse CI on preview URL` ステップの env: `MEASURE_URL: https://kaden-kaimi.pages.dev`
- 対象 URL: `https://kaden-kaimi.pages.dev/`、`https://kaden-kaimi.pages.dev/washers/panasonic-na-lx129dl/`
- 各 URL × 3 runs = 6 runs 全成功（`Run #1...done.` ～ `Run #3...done.`、`Done running Lighthouse!`）
- `Checking assertions against 2 URL(s), 6 total run(s)` → `Done running autorun.`（assertion error なし → `.lighthouserc.preview.json` のしきい値を全て満たした）

### 受け入れ条件達成状況

| 条件 | 結果 |
|---|---|
| workflow が起動する | ✅ run 24979704717 |
| `in_progress` を待ち `success` で抜ける | ✅ Attempt 1-2 → 3 で抜け |
| `Fallback used: false` | ✅ `is_fallback=false` 経路で完了 |
| `failure` 系で fail | ⏸ 今回 success のみ通過。fail 経路の実走確認は異常発生時に委ねる |
| `uses-http2` audit pass | ✅ assertion error なし |
| `architecture.md` / `README.md` 更新 | ✅ origin/main で確認 |

### 副次発見: artifact upload の no-files warning

`Upload Lighthouse artifacts` ステップで以下の warning が発生（ステップ自体は success）:

```
##[warning]No files were found with the provided path: .lighthouseci/. No artifacts will be uploaded.
```

`lhci autorun` は計測・assertion とも成功しているが、`.lighthouseci/` ディレクトリに upload 対象ファイルが残っていない。`assertMatrix` だけ走らせると LHR JSON を残さないなど lhci の挙動依存の可能性。受け入れ条件には含まれないため本セッションでは fix せず、`tasks.json` に **T-008**（pending）として登録し follow-up とする。PR #4（旧 spec）の workflow 新設時から潜在していた問題で、Check Runs polling 切替（PR #6）と無関係。

### 旧 spec の archived 化

`20260426-lighthouse-ci-two-tier` の物理ファイルは依然 phase-2-dryrun ブランチにのみ存在。archived 化は phase-2-dryrun が main にマージされた**後**、別 PR で実施する（前回 devlog の記載通り）。

### 残タスク

- T-008: `Upload Lighthouse artifacts` の no-files warning 解消（pending、PR #4 由来の潜在問題）
- 旧 spec archived 化（phase-2-dryrun merge 後）

## T-008: Upload Lighthouse artifacts no-files warning 修正

ブランチ `fix/lighthouse-artifact-upload` を `origin/main` から切って実施。

### 原因特定

run 24979704717 の `Run Lighthouse CI on preview URL` ステップ末尾は以下:

```
Uploading median LHR of https://kaden-kaimi.pages.dev/...success!
Open the report at https://storage.googleapis.com/lighthouse-infrastructure.appspot.com/reports/1777270990554-81855.report.html
Uploading median LHR of https://kaden-kaimi.pages.dev/washers/panasonic-na-lx129dl/...success!
Open the report at https://storage.googleapis.com/lighthouse-infrastructure.appspot.com/reports/1777270990857-2983.report.html
No GitHub token set, skipping GitHub status check.

Done running autorun.
```

`Healthcheck passed!` 段で `✅ .lighthouseci/ directory writable` と出ており、`lhci collect` は `.lighthouseci/lhr-*.json/html` を生成しているはず。ローカル `.lighthouseci/` には過去 run の `lhr-1777032855075.{html,json}` 等が実在し、`lhci autorun` の出力先は `.lighthouseci/` で確定。

→ 原因は **lhci 側ではなく `actions/upload-artifact@v4` の glob 挙動**。v4 はデフォルトで dot 始まりパス（hidden file/dir）を除外する仕様であり、`.lighthouseci/` ディレクトリ自体が `.` で始まるため glob から脱落し "No files were found" warning となる。`actions/upload-artifact` README で明示されており、`include-hidden-files: true` で対象に含められる。

### 修正

`.github/workflows/lighthouse-preview.yml` の Upload ステップに `include-hidden-files: true` を追加。`.lighthouseci/` 配下の `lhr-*.{json,html}` / `links.json` / `assertion-results.json` は計測結果のみで機密情報を含まないため、hidden 制限を解除して問題ない。

```yaml
- name: Upload Lighthouse artifacts
  if: always()
  uses: actions/upload-artifact@v4
  with:
    name: lighthouse-preview-reports${{ ... }}
    path: .lighthouseci/
    retention-days: 14
    include-hidden-files: true   # ← 追加
```

### 検証方針

- 受け入れ条件: マージ後の Lighthouse CI (preview URL) run で `lighthouse-preview-reports`（または `-fallback`）artifact が GitHub Actions 上に保存される
- 検証コマンド: `gh api repos/Ryo722/kaden-kaimi/actions/runs/<id>/artifacts` で `total_count >= 1`
- マージ後の verify step で T-008 を `state: implemented` + `verified_at` 記録に更新する
