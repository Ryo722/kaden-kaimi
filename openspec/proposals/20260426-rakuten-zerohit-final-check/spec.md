---
id: 20260426-rakuten-zerohit-final-check
title: 楽天 API keyword 検索ヒット 0 件問題の最終検証 + フォールバック確認
status: implemented
created: 2026-04-26
implemented: 2026-04-27
risk: 中
deprecation_when: "全 15 機種で楽天/Yahoo! 双方の安定ヒットが 7 日連続観測できた時点で archived"
---

## なぜ（背景）

- P2.5.A4 で楽天 keyword 検索が `keyword=NA-LX129DL` で 0 件、Yahoo! は部品ショップが top hit となる事象が発覚
- P2.5.D（価格下限フィルタ ¥50,000）+ P2.5.E（brand 名併記 keyword）で対策実装済み（commit `78c66a0` 等）
- ただし P2.5.E5「再ドライランで楽天もヒットすることを確認」が **未実施（ユーザー作業）**
- 万一 brand 名併記でもヒットしない場合のフォールバック設計が未定（バグ修正型タスク）

## delta（変更仕様）

<!-- MODIFIED -->
- `phase-2-dryrun` ブランチで再ドライラン実行、楽天 API のヒット件数 / 採用価格を記録（既存 workflow への変更なし、運用作業）
- `docs/devlog/2026-04-26.md`（または翌日付）に「P2.5.E5 再ドライラン結果」セクションを追加し、楽天/Yahoo! 双方の hit 件数と採用価格を残す

<!-- ADDED -->
- 楽天 0 件継続時のフォールバック動作を `workers/fetch-prices/src/rakuten.ts` の docstring に明記する（コメント追加のみ、ロジック変更なし）。フォールバック仕様: 「brand 名併記でも 0 件の場合、`null` を返し pipeline は Yahoo! 側のみで PriceRecord を成立させる（既存挙動の文書化）」
- 任意: 7 日連続で楽天 0 件が観測された機種を Workers ログから検出するスクリプトの設計メモを `docs/devlog/` に書き残す（実装は別 spec）

<!-- REMOVED -->
- なし

## 制約・非スコープ

- 楽天 API のヒットアルゴリズム自体は変更しない（API 仕様の影響範囲が広いため）
- フォールバック挙動の自動化（例: メール通知）は本 spec の対象外
- 価格.com 等の他データソース追加は規約違反リスクのため恒久的にスコープ外

## 受け入れ条件

- [ ] `phase-2-dryrun` ブランチで再ドライラン実施、楽天/Yahoo! のヒット件数を記録
- [ ] 楽天が 0 件継続なら、フォールバック挙動が docstring に明記され、devlog にも残っている
- [ ] 既存テスト 200 件すべて pass（ロジック変更がないため自動的に満たされる想定だが、念のため確認）

## 設計判断

- 「再現テスト + 統合確認」が必要だが、ロジック変更ではなく観測なので、既存テストの再実行で代替する
- バグ修正の本質は「実 API 環境での挙動確認」と「フォールバック仕様の明文化」
- Workers ログを後から分析可能にすることで、次フェーズで自動アラートに繋げやすくする
